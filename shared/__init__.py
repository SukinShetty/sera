# shared/__init__.py
# This file makes 'shared' a Python package so other parts of SERA can import from it.
# Example: from shared.config import CONFIG
#          from shared.file_io import read_markdown, write_markdown
