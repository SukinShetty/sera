# P4 Evidence — Research Memory

> **Update 2026-07-07:** credits restored; the pending live evidence was
> completed in the P6 session — see "Pending live evidence — COMPLETED"
> at the bottom. Original blocked-status record kept below unchanged.

**Date:** 2026-07-07
**Session goal:** outcome ledger + memory-conditioned hypothesis generation
+ condition-vote answers + higher-is-better metric contract.

**Status:** all feature work (Tasks 1–7) is complete and tested —
`120 passed`. The live evidence runs (Task 8) are blocked: the Anthropic
API key is valid but the account has insufficient credits.

## What happened on the live run

Command:

```
py -3.14 -m cli.main ask "Which JSON parsing approach is fastest for small payloads: stdlib json, manual string slicing, or regex extraction?"
```

Every API call failed with:

```
Error code: 400 - {'type': 'error', 'error': {'type': 'invalid_request_error',
'message': 'Your credit balance is too low to access the Anthropic API.
Please go to Plans & Billing to upgrade or purchase credits.'}}
```

Observed behavior (all correct, and itself partial evidence the wiring works):

- Hypothesis generation degraded gracefully to the offline fallback
  (fallback titles, each carrying its `predicted_winner`).
- scriptgen refused to fake anything: each hypothesis FAILED loudly with
  the API error; the ask continued past individual failures.
- The all-fail path errored the command with the designed message:
  `Error: All 3 experiments failed after repairs. See the scriptgen-*.log
  files in engine/logs/ask-json-parsing-approach-fastest`.
- **The outcome ledger recorded all 3 outcomes live** with
  `verdict: "failed"` — the first real entries ever written through the
  full `ask → ledger.record` path.

The billing-failure artifacts (ledger entries, client vault, logs) were
removed afterward: infrastructure failures are not research outcomes and
would have polluted future memory queries about JSON parsing.

## What is proven by the test suite (no API needed)

- Ledger record / query relevance / stats math (`tests/test_memory.py::TestLedger`)
- Verdict computation survived/killed/failed (`TestVerdict`)
- `memory=True` injects the PAST RESEARCH OUTCOMES block into the actual
  prompt payload; `memory=False` injects nothing; both are audit-logged
  (`TestMemoryInjection`, asserted on a mocked Anthropic client)
- Condition vote: 2-of-3 majority beats a higher incommensurable raw
  value; tie-break by mean normalized margin (`TestConditionVote`)
- `validate_script` rejects `neg_*` metric names (`TestNegMetricRejected`)

Full pytest tail:

```
120 passed in 3.91s
```

## To produce the pending live evidence

Top up API credits, then:

```
py -3.14 -m cli.main ask "Which JSON parsing approach is fastest for small payloads: stdlib json, manual string slicing, or regex extraction?"
py -3.14 -m cli.main ask "Which JSON parsing approach is most robust to malformed input: stdlib json with try/except, manual slicing, or regex extraction?"
# proof of memory conditioning:
#   type engine\logs\ask-json-parsing-approach-robust\hypgen-brief-001-001.log
py -3.14 -m cli.main memory stats
```

and replace this section with the outputs.

---

## Pending live evidence — COMPLETED (2026-07-07)

### Ask 1 — "fastest JSON parsing" (memory on, fresh ledger)

```
Created client vault: ask-json-parsing-approach-fastest
OK Brief written: brief-001
OK 3 hypotheses generated (memory on)
[1/3] Testing: stdlib json fastest for small payloads (predicts: stdlib_json)
    done winner=stdlib_json (throughput_ops_per_sec=746589.9)
[2/3] Testing: manual string slicing beats stdlib json (predicts: manual_string_slicing)
    FAILED [SERA ScriptGen] Could not produce a working script for 'exp-002' after 3 attempts
[3/3] Testing: regex extraction slowest due to compilation cost (predicts: stdlib_json)
    FAILED [SERA ScriptGen] Could not produce a working script for 'exp-003' after 3 attempts
┌────────────────────────────────── ANSWER ───────────────────────────────────┐
│ stdlib_json won 1 of 1 experiments (condition vote; mean normalized margin  │
│ 0.8285)                                                                     │
└──────────────────────────────────────────────────────────────────────────────┘
Evidence: 1/3 experiments succeeded
```

The two failures exposed two real scriptgen bugs, fixed before the P6
ablation: (1) `validate_script` falsely rejected `re.compile()` (Attribute
calls are now allowed — the import allowlist already blocks dangerous
modules; only bare builtin calls are forbidden); (2) `max_tokens=4000`
truncated longer scripts (now 8000 + a compactness/ASCII instruction).

### Ask 2 — related "robust JSON parsing" question (memory conditioned it)

```
Created client vault: ask-json-parsing-approach-robust
OK 3 hypotheses generated (memory on)
[1/3] Testing: Stdlib JSON try/except handles malformed input best (predicts: stdlib_json_try_except)
    done winner=stdlib_json_try_except (composite_robustness_score=100.0)
[2/3] Testing: Regex extraction tolerates partial or wrapped malformed JSON (predicts: regex_extraction)
    done winner=manual_slicing (composite_robustness_score=40.06)
[3/3] Testing: Manual slicing fails most on deeply malformed inputs (predicts: stdlib_json_try_except)
    done winner=manual_string_slicing (robustness_score=69.0)
┌────────────────────────────────── ANSWER ───────────────────────────────────┐
│ stdlib_json_try_except won 1 of 3 experiments (condition vote; mean         │
│ normalized margin 0.6667)                                                   │
└──────────────────────────────────────────────────────────────────────────────┘
Evidence: 3/3 experiments succeeded
```

Note: two of ask 2's experiments self-labeled **measurement** (real
algorithms on real malformed inputs), only one simulation.

### PROOF of memory conditioning — the exact injected block
(`engine/logs/ask-json-parsing-approach-robust/hypgen-brief-001-001.log`)

```
injected memory block:
PAST RESEARCH OUTCOMES (from prior experiments — calibrate on these):
- Q: Which JSON parsing approach is fastest for small payloads: stdlib json, manual string slicing, or regex extraction? | predicted: stdlib_json | actual winner: stdlib_json | verdict: survived
- Q: Which JSON parsing approach is fastest for small payloads: stdlib json, manual string slicing, or regex extraction? | predicted: manual_string_slicing | actual winner: None | verdict: failed
- Q: Which JSON parsing approach is fastest for small payloads: stdlib json, manual string slicing, or regex extraction? | predicted: stdlib_json | actual winner: None | verdict: failed
```

Ask 2's second run was generated with ask 1's real outcomes in-context.

### `sera memory stats`

```
   SERA Outcome Memory
┌────────────────┬───────┐
│ Total outcomes │     6 │
│ Survived       │     2 │
│ Killed         │     2 │
│ Failed         │     2 │
│ Survival rate  │ 50.0% │
└────────────────┴───────┘
              Outcomes per Client
┌───────────────────────────────────┬──────────┐
│ ask-json-parsing-approach-fastest │        3 │
│ ask-json-parsing-approach-robust  │        3 │
└───────────────────────────────────┴──────────┘
```

## git diff --stat

```
 .gitignore             |   1 +
 cli/commands/ask.py    |  79 +++++++++--
 cli/commands/memory.py |  63 +++++++++
 cli/main.py            |   2 +
 docs/evidence/p4.md    |  79 +++++++++++
 engine/hypothesis.py   |  75 +++++++++-
 engine/ledger.py       | 133 ++++++++++++++++++
 engine/scriptgen.py    |  23 +++-
 tests/test_ask.py      |   3 +
 tests/test_memory.py   | 365 +++++++++++++++++++++++++++++++++++++++++++++++++
 10 files changed, 806 insertions(+), 17 deletions(-)
```
